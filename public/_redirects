# Cloudflare Pages — Redirects
# Formato: [origem] [destino] [código HTTP]

# Redireciona URLs antigas (sem traço) para URLs canônicas (com traço)
/musicadeamor               /musica-de-amor            301
/musicaparamaes             /musica-para-mae           301
/musica-para-maes           /musica-para-mae           301

# Normaliza www → sem www (evita conteúdo duplicado)
# (configure também no painel Cloudflare > Rules > Redirect Rules)

# SPA fallback — todas as rotas não encontradas vão para o index.html
# Isso permite que o React Router / lógica do App.js cuide do roteamento
/*                          /index.html                200
